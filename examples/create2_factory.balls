#define macro FACTORY = {
    salt = calldataload(returndatasize())
    code_size = sub(calldatasize(), 0x20)
    calldatacopy(returndatasize(), 0x20, code_size)
    result = create2(callvalue(), returndatasize(), code_size, salt)
} -> [result]
